import styled from 'styled-components';

export const Container = styled.button`
        color: var(--color-grey-alternative);
        font-size: 1rem;
        background-color: transparent ;
        border: none ;

        &:hover{
            color: var(--color-blue-secundary);
        }
`